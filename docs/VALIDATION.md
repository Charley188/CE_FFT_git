# 本轮验证（2026-09-23）

- 共享生产算法与控制 RTL：保持原算法和控制行为。sys_top 将单/双路宏移到共享 ce_config.vh；ce_fft_core 新增仅仿真的复位期时钟事件优化，综合时仍使用原时钟。其余算法/控制 RTL 逐字节保留。
- ADDA：BD validate/generate 通过；全部顶层直接源文件存在；Vivado 2023.2 RTL 展开通过，0 Error、0 Critical Warning，597 条继承的普通 Warning。不是综合/布局布线时序签核。
- TB：真实 XFFT、完整 10 引擎、实际在线系数控制器和 XPM FIFO 编译展开通过；原不加速运行在复位期耗时过长，主动停止，不记为通过。新增仅复位期时钟事件优化后，21,504 点 H=1 主干通过，单路完整10引擎、在线加载和CRC、连续输出、fault检查均通过。真实运行约17分30秒；本轮编译加运行约22分29秒。
- 新工程端到端：MATLAB 模式一 → Vivado 真实IP主干 → MATLAB 模式二全部通过。21,504 点最大 I/Q 分量误差 0 LSB、复数 RMS 误差 0；相对浮点参考 EVM 约0.294284%。数值比对和绘图均执行成功。
- MATLAB：官方 FFT C 模型 smoke test 通过；H=1 和复数三 tap 定点模型运行通过。另与原工程保存的真实 XFFT 28/20 三 tap 单引擎输出交叉校对：14,336 点，最大分量误差 0 LSB。这是历史输出校对，不替代本轮主干运行。
- MATLAB 比对器：独立构造的数据完全一致时通过；单点 +1 LSB 能定位到样点 41；输出周期断流能拒绝。该项是比对器测试，不冒充 RTL 仿真结果。
- VNA：MAT 和 CSV（含非零 RF 中心频率）输入验证通过；H=1 导出通过；合成复数响应的模式2优化和18位MEM导出通过，预测 ripple 约0.0756 dB；NaN 测量数据被拒绝。合成数据仅用于开发验证，未作为部署校准结果。
- 新工程没有生成 bit/XSA/ELF，没有完成新整板布局布线或上板测量。PS 源码保留，需匹配新 XSA 在 Vitis GUI 中创建/更新平台后构建。
- 双路 RTL 分支保留，默认单路；本轮完整真实 IP 双路运行尚未验证。

开发验证使用内部批处理以收集记录；日常交付入口仍为 Vivado 点击仿真、MATLAB 修改 MODE 后点击 Run，不要求用户执行命令行。
